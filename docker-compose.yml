# KNG3 image: SHAMAN v1 or PRST1 UP. Copy .env.example -> .env (set BOT_STRATEGY_MODE).
services:
  bot:
    build: .
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./logs:/app/logs
      - ./exports:/app/exports
