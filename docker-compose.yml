# SHAMAN v1 image. Copy .env.example -> .env (BOT_STRATEGY_MODE=shaman_v1).
services:
  bot:
    build: .
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./logs:/app/logs
      - ./exports:/app/exports
