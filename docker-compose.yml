# Ships with PALADIN v9: copy .env.example -> .env (BOT_STRATEGY_MODE=paladin_v9) unless you override.
services:
  bot:
    build: .
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./logs:/app/logs
      - ./exports:/app/exports
