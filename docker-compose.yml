# Ships PALADIN v9 by default; optional BOT_STRATEGY_MODE=paladin_v7 or shaman_v1. Copy .env.example -> .env.
services:
  bot:
    build: .
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./logs:/app/logs
      - ./exports:/app/exports
